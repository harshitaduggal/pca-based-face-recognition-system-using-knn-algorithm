# pca-based-face-recognition-system-using-knn-algorithm
A basic face recognition system using PCA for dimensionality reduction and KNN for classification. Face images are converted into low-dimensional Eigenface features using PCA and classified based on nearest neighbors in feature space. Implemented in Python using standard ML libraries, focusing only on core functionality.
